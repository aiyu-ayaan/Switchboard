# Switchboard Documentation

Docusaurus documentation portal for Switchboard's architecture, hardware subsystems (DDC/CI, WASAPI), protocols, zero-trust cryptographic pairing, and operations.

\\\ash
npm install
npm start        # http://localhost:3000
npm run build     # static site -> build/
\\\

Deployed to GitHub Pages by \.github/workflows/docs.yml\ on any \master\ commit whose subject starts with \!docs\ or via manual \workflow_dispatch\.
